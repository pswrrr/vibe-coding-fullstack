<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vibeapp.post.PostMapper">

    <select id="findAll" resultType="Post">
        SELECT * FROM POSTS ORDER BY created_at DESC
    </select>

    <select id="findByNo" resultType="Post">
        SELECT * FROM POSTS WHERE no = #{no}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO POSTS (title, content, views, created_at, updated_at)
        VALUES (#{title}, #{content}, #{views}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE POSTS 
        SET title = #{title}, 
            content = #{content}, 
            views = #{views}, 
            updated_at = #{updatedAt}
        WHERE no = #{no}
    </update>

    <delete id="deleteByNo">
        DELETE FROM POSTS WHERE no = #{no}
    </delete>

</mapper>
